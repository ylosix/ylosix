%li{id: 'product_products_categories_input', class: 'check_boxes input optional'}
  %fieldset.choices
    %legend.label
      = label_tag 'categories', "#{t('activerecord.models.category.other')} | #{root_category.name}"

    %ol.choices-group
      = render partial: 'admin/products/categories_sub_choices', locals: { products_categories: products_categories, categories: root_category.children }
